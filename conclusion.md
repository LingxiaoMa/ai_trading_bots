### 1. 为什么所有算法都不如 Buy & Hold，以及为什么 B&H 不鲁棒
B&H 胜出是测试集数据特性决定的，不是策略本身的优越性。

测试期（2020-2022）BTC 从 $7,200 涨到 $43,000，约 6 倍涨幅，且整体趋势单边向上。在这种行情下，最优策略就是什么都不做——任何卖出信号都意味着踏空后续涨幅，加上每次交易还要付 3% 手续费，主动交易策略先天处于劣势。

但 B&H 不鲁棒：换一个测试集结论会完全相反。2018 年熊市 BTC 从 $20,000 跌到 $3,500，B&H 亏损 82%，而能够识别下跌趋势的交易算法会大幅跑赢。B&H 的胜出完全依赖于测试期恰好是牛市这一偶然条件，它没有任何机制适应不同市场环境。

### 2. 为什么单段时 SA 优于种群算法
SA 单段测试 $4,776，优于 ALO $2,773 和 CS $2,205。这看起来反直觉——单状态算法为什么赢过 30 个体的种群算法？

原因恰恰是 SA 的"弱点"在这里成了优势：SA 冷却率 0.98 使它很快停止探索，只找到一个"够好"的局部最优。这个不够精细的解反而不会过度拟合训练集的细节。

对比种群算法：

ALO 用轮盘赌选择 + 边界收缩，30 个体持续向最优区域聚集，深度挖掘训练集，找到训练值 $53,092 的高度拟合解——但这个解过于"定制化"，到了测试集只剩 $2,773
CS 用 Lévy 飞行广泛探索，找到 $37,920 的训练值，同样存在过拟合
本质上是偏差-方差权衡：SA 高偏差（探索少）但低方差（不过拟合），在单段下泛化反而更好。

### 3. 为什么多段时种群算法优于 SA
多段 fitness 改变了优化目标，要求参数在 4 个不同市场阶段的平均表现都好。这是一个结构完全不同的 fitness landscape，对应参数空间里不同的局部最优。

SA 的问题：单状态算法，一旦快速收敛就只剩一个解。多段 fitness 的局部最优和单段不同，SA 锁定在新局部最优后没有任何机制检验它是否也适合测试集——结果从 $4,776 跌到 $1,422。

种群算法的优势：始终维护 30 个候选解，分布在参数空间的不同位置。即使以多段 fitness 引导，种群的多样性使得其中一些解碰巧在单段全序列和测试集上也表现合理，最终汇报的最优解来自 30 个候选，概率上更可能找到泛化更好的参数。

### 4. 为什么 CS 优于 ALO
单段下 ALO 略好（$2,773 vs $2,205），多段下 CS 大幅胜出（$4,505 vs $2,046）。多段是更严格、更可靠的评估，因此 CS 是更优的算法。

根本差异在于多样性保持机制：

ALO 有两个相互叠加的收敛压力：轮盘赌选择把种群不断拉向当前最优，边界收缩持续压缩搜索范围。在单段嘈杂的 fitness 下，这种压力让它过拟合到训练集；换成多段更复杂的 fitness landscape，同样的机制让它过拟合到多段平均，却仍然没有找到真正泛化的参数。

CS 的 Lévy 飞行步长服从重尾分布，绝大多数时候做小范围精细搜索，但偶尔产生极大跳跃。数学上已证明这是在随机环境中搜索的接近最优策略。加上 pa=0.25 的巢穴替换机制主动注入新解，CS 始终保持探索多样性。多段 fitness 有 4 个市场阶段的约束，搜索空间更复杂——Lévy 飞行的全局探索能力在这种环境下优势更加明显，最终找到了真正跨 regime 鲁棒的参数。

从过拟合比率可以直接看出这一点：

｜ 训练 | 测试	| 比率 |
| --- | --- | --- |
| ALO 多段 | $38,419 | $2,046 | 18.8× | 
| CS 多段 | $28,530 | $4,505 | 6.3× |

CS 的训练值更低，但过拟合比率只有 ALO 的三分之一，说明 Lévy 飞行找到的是真正泛化的解，而非训练集特有的解。